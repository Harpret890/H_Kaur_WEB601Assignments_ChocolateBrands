import { HovereffectsDirective } from './hovereffects.directive';

describe('HovereffectsDirective', () => {
  it('should create an instance', () => {
    const directive = new HovereffectsDirective();
    expect(directive).toBeTruthy();
  });
});
